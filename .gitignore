# macOS
.DS_Store

# Worktrees Claude Code (dépôts imbriqués — ne jamais suivre)
.claude/

# Secrets PKI de la flotte (mTLS, CIN-078) — JAMAIS committer une clé privée.
kiosk/pki/ca/
kiosk/pki/certs/
*.key
*.p12
*.pem

# Secrets DEVICE de la flotte (mots de passe des comptes borne) — JAMAIS committer.
kiosk/devices.json
kiosk/**/devices.json
**/device.json

# Documents internes de conception (non publics) — gardés en local uniquement.
# Contiennent la stratégie produit/technique/QA et ne doivent pas être publiés.
/ARCHITECTURE.md
/ARCHITECTURE-CHANGELOG.md
/SPEC.md
/SPEC-DECISIONS.md
/OPS.md
/QA.md
# Carte de navigation (IA du back-office) — matière de la session UX, interne.
/NAV-MAP.md
# Ticketing léger interne (en attendant GitHub Issues).
/TODO.md
/BACKLOG.md
# Runbook d'actions humaines (infra Supabase/Cloudflare) — local, non public.
/ACTIONS-BERANGER.md
# Prompts de délégation aux agents parallèles — locaux.
/AGENT-PROMPT-CIN045.md
/AGENT-PROMPT-F19-DASHBOARD.md

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*

# Diagnostic reports (https://nodejs.org/api/report.html)
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage
*.lcov

# nyc test coverage
.nyc_output

# Grunt intermediate storage (https://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# Bower dependency directory (https://bower.io/)
bower_components

# node-waf configuration
.lock-wscript

# Compiled binary addons (https://nodejs.org/api/addons.html)
build/Release

# Dependency directories
node_modules/
jspm_packages/

# Snowpack dependency directory (https://snowpack.dev/)
web_modules/

# TypeScript cache
*.tsbuildinfo

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional stylelint cache
.stylelintcache

# Microbundle cache
.rpt2_cache/
.rts2_cache_cjs/
.rts2_cache_es/
.rts2_cache_umd/

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# parcel-bundler cache (https://parceljs.org/)
.cache
.parcel-cache

# Next.js build output
.next
out

# Nuxt.js build / generate output
.nuxt
dist

# Gatsby files
.cache/
# Comment in the public line in if your project uses Gatsby and not Next.js
# https://nextjs.org/blog/next-9-1#public-directory-support
# public

# vuepress build output
.vuepress/dist

# vuepress v2.x temp and cache directory
.temp
.cache

# Docusaurus cache and generated files
.docusaurus

# Serverless directories
.serverless/

# FuseBox cache
.fusebox/

# DynamoDB Local files
.dynamodb/

# TernJS port file
.tern-port

# Stores VSCode versions used for testing VSCode extensions
.vscode-test

# yarn v2
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz
.pnp.*

# Médias de test (Wikimedia Commons) — retéléchargeables, jamais dans git (poids binaire).
# Les .vtt de démo, eux, restent suivis (légers + utiles au POC sous-titres).
booth-client/samples/*.webm
booth-client/samples/*.mp4
# Registre de tickets (spec de construction) — local, non public.
/BACKLOG-SYSTEM.md
/backlog/
/backlog.html
