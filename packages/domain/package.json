{
  "name": "@kioskoscope/domain",
  "version": "0.1.0",
  "private": true,
  "description": "Modèle de domaine canonique partagé (Kiosks, back-office, backend).",
  "type": "module",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "esbuild scripts/domain_smoke.ts --bundle --platform=node --format=esm --outfile=node_modules/.cache/domain_smoke.mjs && node node_modules/.cache/domain_smoke.mjs"
  }
}
